import { Router } from 'express';
import adminSettingsRouter from './admin-settings';

const router = Router();

// This is a router specifically for API setup routes
// The admin routes are already directly set up in the main routes.ts file

export default router;